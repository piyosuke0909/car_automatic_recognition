# parking-video-analyzer 実装手順書

## 目的

MP4動画 `input/parking_sample.mp4` を入力として、車両検出、簡易追跡、エリア別混雑集計を行い、解析JSONと確認用アノテーション動画を出力するMVPを段階的に作成する。

この手順書では実装前に、どの順番で作業を進めるかを定義する。現時点ではソースコード実装、モデル取得、動画解析は行わない。

## 前提

- Unityプロジェクトとは別のPythonプロジェクトとして `parking-video-analyzer/` を作成する。
- 座標は駐車場座標へ変換せず、画像座標 `x, y` のまま扱う。
- 入力動画は `parking-video-analyzer/input/parking_sample.mp4` に配置する。
- 出力先は `parking-video-analyzer/output/` とする。
- 実行コマンドは `python src/main.py` とする。

## 想定ディレクトリ構成

```text
parking-video-analyzer/
├─ input/
│  └─ parking_sample.mp4
├─ output/
│  ├─ analysis_result.json
│  └─ annotated_video.mp4
├─ src/
│  ├─ main.py
│  ├─ vehicle_detector.py
│  ├─ vehicle_tracker.py
│  ├─ area_analyzer.py
│  ├─ route_analyzer.py
│  ├─ json_exporter.py
│  └─ config.py
├─ requirements.txt
└─ README.md
```

## 実装の進め方

### 1. プロジェクト雛形を作成する

- `parking-video-analyzer/` を作成する。
- `input/`, `output/`, `src/` を作成する。
- `requirements.txt` を作成する。
- `README.md` を整備する。
- `input/parking_sample.mp4` はユーザーが配置する前提にする。

### 2. 設定値を `src/config.py` に集約する

以下の設定を `config.py` に定義する。

- 入力動画パス
- 出力JSONパス
- 出力動画パス
- YOLOモデル名
- 検出対象クラス
- 信頼度しきい値
- エリア分割数
- 描画色
- 混雑スコア計算式
- heatLevelのしきい値

最初は固定値でよい。コマンドライン引数化はMVP後の拡張に回す。

### 3. 動画読み込み処理を `src/main.py` に作る

- OpenCVで `input/parking_sample.mp4` を開く。
- FPS、フレーム数、幅、高さを取得する。
- 読み込めない場合はエラーメッセージを出して終了する。
- 出力動画用に `cv2.VideoWriter` を初期化する。

この段階では動画を読み込み、同じフレームを書き出せることを確認する。

### 4. YOLO検出処理を `src/vehicle_detector.py` に分離する

- Ultralytics YOLOを初期化する。
- 各フレームに対して推論を実行する。
- `car`, `truck`, `bus` のみを抽出する。
- bbox、label、confidenceを標準化した辞書として返す。

検出結果の形式は、後続処理で扱いやすいように統一する。

```json
{
  "bbox": [x1, y1, x2, y2],
  "center": {"x": 0, "y": 0},
  "label": "car",
  "confidence": 0.95
}
```

### 5. 簡易追跡処理を `src/vehicle_tracker.py` に作る

MVPではDeepSORTなどの外部トラッカーは使わず、中心点距離ベースの簡易追跡で `vehicleId` を付与する。

- 前フレームの車両中心点と現フレームの検出中心点を比較する。
- 距離が一定以下の検出を同じ車両とみなす。
- 対応が取れない検出には新しい `vehicleId` を付与する。
- 一定フレーム以上見失った車両は追跡対象から外す。

MVP後に必要であれば、ByteTrackやSORTへ差し替える。

### 6. エリア判定を `src/area_analyzer.py` に作る

- 動画画面を画像座標上でグリッド分割する。
- 各エリアに `areaId` を付与する。
- 車両の中心点がどのエリア内にあるかを判定する。
- フレームごとにエリア別の現在車両数を集計する。

初期案は、画面を横3列、縦2行に分ける。

```text
area_1 | area_2 | area_3
area_4 | area_5 | area_6
```

### 7. 経路生成を `src/route_analyzer.py` に作る

- `vehicleId` ごとに各フレームの中心点を蓄積する。
- 蓄積した中心点列を `route` として出力する。
- 各点には `frameIndex`, `x`, `y`, `areaId` を含める。

最初はスムージングや滞在判定は行わない。

### 8. JSON出力を `src/json_exporter.py` に分離する

最終的に以下のトップレベル形式で `output/analysis_result.json` を出力する。

```json
{
  "source": "mp4_video",
  "videoFile": "input/parking_sample.mp4",
  "fps": 30,
  "frameCount": 0,
  "duration": 0,
  "vehicles": [],
  "frames": [],
  "areas": []
}
```

`vehicles`, `frames`, `areas` の詳細形式はREADMEの要件定義に従う。

### 9. 混雑スコアを計算する

エリアごとに以下を算出する。

- `averageCarCount`
- `maxCarCount`
- `congestionScore`
- `heatLevel`

計算式は以下とする。

```text
congestionScore = averageCarCount * 10 + maxCarCount * 5
```

`heatLevel` は以下で判定する。

```text
0-30: low
31-60: medium
61以上: high
```

### 10. 確認用動画を描画する

`output/annotated_video.mp4` に以下を描画する。

- 車両のbbox
- `vehicleId`
- label
- confidence
- `areaId`
- エリア境界線
- エリアごとの現在車両数

描画処理はMVPでは `main.py` 内の補助関数から始め、肥大化した場合は別ファイルへ分離する。

### 11. 動作確認を行う

以下を確認する。

- `python src/main.py` で実行できる。
- `output/analysis_result.json` が生成される。
- `output/annotated_video.mp4` が生成される。
- JSONのトップレベルキーが要件と一致する。
- `frames` にフレームごとの検出結果が入る。
- `vehicles` に車両ごとの経路が入る。
- `areas` に混雑集計が入る。
- 確認用動画にbbox、ID、エリア情報が描画される。

## MVP完了条件

- 入力動画を最後まで解析できる。
- `car`, `truck`, `bus` の検出結果だけが出力される。
- 検出車両に `vehicleId` が付与される。
- フレームごとの bbox、center、confidence、areaId がJSONに保存される。
- 車両ごとの route がJSONに保存される。
- エリアごとの混雑スコアと heatLevel がJSONに保存される。
- アノテーション動画で解析内容を目視確認できる。

## MVP後の拡張候補

- ByteTrack、SORT、DeepSORTなどの追跡アルゴリズム導入
- 駐車場平面座標への変換
- エリア定義をJSONやYAMLから読み込む方式への変更
- 車両の滞在時間、進入、退出イベントの抽出
- リアルタイムカメラ入力への対応
- Unity連携用JSON形式への変換
- 処理速度改善とバッチ解析対応
